package org.coinjema.internal.context.names;

public class DepForAliasNameResolver extends AbstractSingleDepResolver {
    public DepForAliasNameResolver(String alias) {
        super(alias);
    }
}
